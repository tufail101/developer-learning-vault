# Practice

## Exercise 1

Create a `CourseCard` component that accepts `title`, `level`, and `duration` props.

## Exercise 2

Render three `CourseCard` components with different values.

## Exercise 3

Refactor a hardcoded section so it receives its content through props instead.
