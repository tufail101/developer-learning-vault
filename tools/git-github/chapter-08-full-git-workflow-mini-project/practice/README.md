# Practice — Full Git Workflow

## Exercise 1
Initialize a fresh repository and make an initial commit with a README file.

## Exercise 2
Create a feature branch, add one new file, commit it, and merge it back into `main`.

## Exercise 3
Push the repository to GitHub and verify that both the README and the new file appear online.

## Done Checklist
- I completed: initialize a fresh repository and make an initial commit with a README file
- I created a feature branch, add one new file, commit it, and merge it back into `main`
- I completed: push the repository to GitHub and verify that both the README and the new file appear online
- I tested the final result myself instead of guessing that it works
