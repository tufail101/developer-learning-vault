# Todo

- Make a personal cheat sheet that compares `restore`, `revert`, and `reset` in plain language.
- Practice these commands in a throwaway repo until you can predict the result before running them.
- Add one reminder to your notes about checking whether a commit has already been pushed before rewriting history.
