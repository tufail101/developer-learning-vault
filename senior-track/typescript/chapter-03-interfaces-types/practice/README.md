# Practice — Interfaces And Types

## Exercise 1
Create an interface for a product with `id`, `name`, and `price`.

## Exercise 2
Create a union type for status values like `loading`, `success`, and `error`.

## Exercise 3
Build one sample object that matches the interface and one function that accepts your union type.

## Done Checklist
- I created an interface for a product with `id`, `name`, and `price`
- I created a union type for status values like `loading`, `success`, and `error`
- I built one sample object that matches the interface and one function that accepts your union type
- I tested the final result myself instead of guessing that it works
