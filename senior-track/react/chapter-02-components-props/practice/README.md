# Practice — Components And Props

## Exercise 1
Create a `ProfileCard` component that accepts `name` and `role` props.

## Exercise 2
Render the same `ProfileCard` three times with different prop values.

## Exercise 3
Add a third prop such as `bio` or `avatarLabel` and confirm each card still renders correctly with different content.

## Done Checklist
- I created a `ProfileCard` component that accepts `name` and `role` props
- I completed: render the same `ProfileCard` three times with different prop values
- I added a third prop such as `bio` or `avatarLabel` and confirm each card still renders correctly with different content
- I tested the final result myself instead of guessing that it works
