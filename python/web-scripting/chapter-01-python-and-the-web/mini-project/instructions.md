# Instructions — Backend Or Browser?

1. Create a file named `backend-or-browser.py`.
2. Make one list for browser jobs and one list for server jobs.
3. Print a clear heading before each list so the output is easy to read.
4. Loop through the browser jobs first, then the server jobs.
5. Add one final sentence that explains Python belongs on the server side in normal web development.

## Suggested Build Order

1. Get one list printing correctly first.
2. Add the second list.
3. Improve the output so it looks organized in the terminal.
4. Run the file with `python3 backend-or-browser.py`.
5. Add the stretch goal only after the basic version is clear and readable.
