# Practice — JavaScript Introduction

## Exercise 1
Open the browser console and log your name, favorite food, and the number `10` using three separate `console.log()` calls.

## Exercise 2
Create a simple HTML file with a linked script file and log `JavaScript is connected` in the browser console.

## Exercise 3
Change the script so it logs one sentence before the page loads and one sentence after you refresh, then explain where you saw the output.

## Done Checklist
- I opened the browser console and log your name, favorite food, and the number `10` using three separate `console.log()` calls
- I created a simple HTML file with a linked script file and log `JavaScript is connected` in the browser console
- I completed: change the script so it logs one sentence before the page loads and one sentence after you refresh, then explain where you saw the output
- I tested the final result myself instead of guessing that it works
