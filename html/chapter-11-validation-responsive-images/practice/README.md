# Practice — Validation And Responsive Images

## Exercise 1
Run one HTML file through an HTML validator and fix the first error you find.

## Exercise 2
Add an image with `width` and `height` attributes or responsive sizing so it behaves better on smaller screens.

## Exercise 3
Create a simple page with two images and test that both still look reasonable when the browser window becomes narrow.

## Done Checklist
- I ran one HTML file through an HTML validator and fix the first error you find
- I added an image with `width` and `height` attributes or responsive sizing so it behaves better on smaller screens
- I created a simple page with two images and test that both still look reasonable when the browser window becomes narrow
- I tested the final result myself instead of guessing that it works
