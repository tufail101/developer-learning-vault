# Practice — useEffect And Lifecycle

## Exercise 1
Use `useEffect` to change `document.title` when a component renders.

## Exercise 2
Add a resize listener inside an effect and clean it up when the component unmounts.

## Exercise 3
Fetch one item from a placeholder API on mount and show loading text until the data arrives.

## Done Checklist
- I used `useEffect` to change `document.title` when a component renders
- I added a resize listener inside an effect and clean it up when the component unmounts
- I completed: fetch one item from a placeholder API on mount and show loading text until the data arrives
- I tested the final result myself instead of guessing that it works
