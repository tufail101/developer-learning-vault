# Practice — JavaScript Mini Project Prep

## Exercise 1
Pick one small UI idea, such as a counter or to-do starter, and list the DOM elements and events it will need.

## Exercise 2
Build the HTML structure for that idea and connect an empty `script.js` file.

## Exercise 3
Implement one interactive feature, such as adding an item or increasing a count, and test it manually in the browser.

## Done Checklist
- I picked one small UI idea, such as a counter or to-do starter, and list the DOM elements and events it will need
- I built the HTML structure for that idea and connect an empty `script.js` file
- I completed: implement one interactive feature, such as adding an item or increasing a count, and test it manually in the browser
- I tested the final result myself instead of guessing that it works
