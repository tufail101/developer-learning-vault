# Practice — Forms

## Exercise 1
Create a contact form with name, email, and message fields.

## Exercise 2
Add labels for every field and make sure each label connects to its input with `for` and `id`.

## Exercise 3
Add a submit button and at least one helpful validation attribute such as `required` or `minlength`.

## Done Checklist
- I created a contact form with name, email, and message fields
- I added labels for every field and make sure each label connects to its input with `for` and `id`
- I added a submit button and at least one helpful validation attribute such as `required` or `minlength`
- I tested the final result myself instead of guessing that it works
