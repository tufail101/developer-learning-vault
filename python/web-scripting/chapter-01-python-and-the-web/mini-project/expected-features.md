# Expected Features — Backend Or Browser?

- The script runs with `python3 backend-or-browser.py`
- The output clearly separates browser jobs from server jobs
- There are at least three jobs listed under each section
- The final summary line explains that Python normally handles server-side work in web development
- The output is readable enough that someone else could understand it without opening the file
