# Practice — CSS Best Practices

## Exercise 1
Refactor one messy CSS file so related styles are grouped together in a clearer order.

## Exercise 2
Replace repeated color values in that file with CSS variables.

## Exercise 3
Rename one or two vague class names so someone reading the HTML can guess their purpose faster.

## Done Checklist
- I completed: refactor one messy CSS file so related styles are grouped together in a clearer order
- I completed: replace repeated color values in that file with CSS variables
- I completed: rename one or two vague class names so someone reading the HTML can guess their purpose faster
- I tested the final result myself instead of guessing that it works
