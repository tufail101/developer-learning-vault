# Practice — TypeScript With React

## Exercise 1
Type the props for a simple `UserCard` React component.

## Exercise 2
Add a typed `useState` value for a controlled input field.

## Exercise 3
Write a typed input change handler with `React.ChangeEvent<HTMLInputElement>` and confirm the component still behaves normally.

## Done Checklist
- I completed: type the props for a simple `UserCard` React component
- I added a typed `useState` value for a controlled input field
- I wrote a typed input change handler with `React.ChangeEvent<HTMLInputElement>` and confirm the component still behaves normally
- I tested the final result myself instead of guessing that it works
