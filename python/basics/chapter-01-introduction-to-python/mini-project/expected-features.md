# Expected Features — First Python Welcome Script

- the script is saved in a `.py` file
- the script runs from the terminal with `python3 filename.py`
- the script prints at least four lines
- the output order matches the code order
- the script is simple enough that a beginner can read every line and know what it does
