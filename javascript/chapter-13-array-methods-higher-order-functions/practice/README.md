# Practice — Array Methods And Higher-Order Functions

## Exercise 1
Use `.map()` to turn an array of prices into a new array with tax added.

## Exercise 2
Use `.filter()` to keep only the items that cost more than 50 from the same array of objects.

## Exercise 3
Use `.reduce()` to calculate the total price of all remaining items and log the final number.

## Done Checklist
- I used `.map()` to turn an array of prices into a new array with tax added
- I used `.filter()` to keep only the items that cost more than 50 from the same array of objects
- I used `.reduce()` to calculate the total price of all remaining items and log the final number
- I tested the final result myself instead of guessing that it works
