# Practice — File System Module

## Exercise 1
Use the Node.js `fs` module to create a text file named `notes.txt` with one short sentence inside it.

## Exercise 2
Read the contents of `notes.txt` and log the text to the terminal.

## Exercise 3
Update the script so it appends a second line to the same file, then read the file again and confirm both lines are there.

## Done Checklist
- I used the Node.js `fs` module to create a text file named `notes.txt` with one short sentence inside it
- I completed: read the contents of `notes.txt` and log the text to the terminal
- I completed: update the script so it appends a second line to the same file, then read the file again and confirm both lines are there
- I tested the final result myself instead of guessing that it works
