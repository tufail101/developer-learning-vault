// This example shows one simple way to describe a MERN project structure in JavaScript.

const projectStructure = {
  root: "mern-app",
  folders: ["client", "server"],
  files: ["package.json"],
};

console.log(projectStructure);
