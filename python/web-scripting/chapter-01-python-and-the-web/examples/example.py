"""This example shows a few server-side jobs Python can do in web development."""

python_jobs = [
    "run a web server",
    "talk to a database",
    "return JSON to the frontend",
]

print("Python usually works on the server side of a web project.\n")

for job in python_jobs:
    print(f"- It can {job}.")

print("\nRun this file with: python3 example.py")
