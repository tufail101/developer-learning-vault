# Practice — DOM Manipulation

## Exercise 1
Create an HTML file with one heading and one paragraph. Use JavaScript to change the heading text after the page loads.

## Exercise 2
Add a button that changes the paragraph text when clicked.

## Exercise 3
Create a second button that toggles a CSS class on a box so you can see the DOM update visually.

## Done Checklist
- I created an HTML file with one heading and one paragraph. Use JavaScript to change the heading text after the page loads
- I added a button that changes the paragraph text when clicked
- I created a second button that toggles a CSS class on a box so you can see the DOM update visually
- I tested the final result myself instead of guessing that it works
