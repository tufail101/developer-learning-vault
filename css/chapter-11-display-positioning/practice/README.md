# Practice — Display And Positioning

## Exercise 1
Create three boxes and change one from block to inline-block so you can compare the layout.

## Exercise 2
Use `position: relative` and `position: absolute` to place a badge in the corner of a card.

## Exercise 3
Add a fixed header or fixed button and test that it stays visible as the page scrolls.

## Done Checklist
- I created three boxes and change one from block to inline-block so you can compare the layout
- I used `position: relative` and `position: absolute` to place a badge in the corner of a card
- I added a fixed header or fixed button and test that it stays visible as the page scrolls
- I tested the final result myself instead of guessing that it works
