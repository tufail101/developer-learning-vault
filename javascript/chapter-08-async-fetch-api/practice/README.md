# Practice — Async And Fetch API

## Exercise 1
Use `fetch()` to request one post from `https://jsonplaceholder.typicode.com/posts/1` and log the result.

## Exercise 2
Display the fetched post title inside the page instead of only logging it.

## Exercise 3
Add a loading message before the request starts and remove it when the data appears.

## Done Checklist
- I used `fetch()` to request one post from `https://jsonplaceholder.typicode.com/posts/1` and log the result
- I completed: display the fetched post title inside the page instead of only logging it
- I added a loading message before the request starts and remove it when the data appears
- I tested the final result myself instead of guessing that it works
