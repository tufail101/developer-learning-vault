# Practice — HTML Introduction

## Exercise 1
Create your first HTML file with `<!DOCTYPE html>`, `<html>`, `<head>`, and `<body>`.

## Exercise 2
Add one heading and two paragraphs that introduce yourself.

## Exercise 3
Change the page title in the `<head>` and confirm the browser tab text updates.

## Done Checklist
- I created your first HTML file with `<!DOCTYPE html>`, `<html>`, `<head>`, and `<body>`
- I added one heading and two paragraphs that introduce yourself
- I completed: change the page title in the `<head>` and confirm the browser tab text updates
- I tested the final result myself instead of guessing that it works
