# Practice — Context API

## Exercise 1
Create a context that stores a theme value of `light` or `dark`.

## Exercise 2
Wrap a small component tree with the provider and read the theme from a nested child.

## Exercise 3
Add a toggle button that updates the context value and confirm every consumer updates too.

## Done Checklist
- I created a context that stores a theme value of `light` or `dark`
- I completed: wrap a small component tree with the provider and read the theme from a nested child
- I added a toggle button that updates the context value and confirm every consumer updates too
- I tested the final result myself instead of guessing that it works
