# Practice — State And useState

## Exercise 1
Build a counter component with increase and decrease buttons. The count should never go below zero.

## Exercise 2
Create a toggle that switches between `Dark Mode` and `Light Mode` text when clicked.

## Exercise 3
Build a controlled input that shows `Hello, [name]` live as the user types.

## Done Checklist
- I built a counter component with increase and decrease buttons. The count should never go below zero
- I created a toggle that switches between `Dark Mode` and `Light Mode` text when clicked
- I built a controlled input that shows `Hello, [name]` live as the user types
- I tested the final result myself instead of guessing that it works
