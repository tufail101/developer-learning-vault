# Practice — tsconfig And Tooling

## Exercise 1
Create a basic `tsconfig.json` with `target`, `module`, and `strict` options.

## Exercise 2
Change one compiler option, such as `strict`, and explain in your own words what that option is trying to control.

## Exercise 3
Add a short note below the file explaining why your chosen settings make sense for a small React or Node practice project.

## Done Checklist
- I created a basic `tsconfig.json` with `target`, `module`, and `strict` options
- I completed: change one compiler option, such as `strict`, and explain in your own words what that option is trying to control
- I added a short note below the file explaining why your chosen settings make sense for a small React or Node practice project
- I tested the final result myself instead of guessing that it works
