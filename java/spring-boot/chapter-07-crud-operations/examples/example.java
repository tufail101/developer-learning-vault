// This example prints a simple CRUD route list.
class Example {
    public static void main(String[] args) {
        String[] actions = {
            "create one item",
            "read one or many items",
            "update one item",
            "delete one item"
        };

        for (String action : actions) {
            System.out.println(action);
        }
    }
}
