# Expected Features — Personal Profile Script

- the script stores profile details in variables instead of hard-coding everything directly in `print()`
- the script includes at least one string, one number, and one boolean
- each printed line has a clear label
- the output is easy to read in the terminal
- the learner can point to each variable and explain what type of value it stores
