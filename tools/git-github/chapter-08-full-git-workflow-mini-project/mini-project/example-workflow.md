# Example Mini Project Workflow

1. Create `my-git-practice/`
2. Run `git init`
3. Add `README.md`
4. Commit `Initial commit`
5. Create branch `feature-about`
6. Add more content and commit
7. Merge into `main`
8. Create GitHub repo
9. Push with `git push -u origin main`
