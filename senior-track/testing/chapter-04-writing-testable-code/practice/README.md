# Practice — Writing Testable Code

## Exercise 1
Write one pure function that always returns the same result for the same input.

## Exercise 2
Refactor one function so it accepts a dependency as an argument instead of hardcoding it.

## Exercise 3
Split one small feature into a logic function and a UI wiring function, then explain which part became easier to test.

## Done Checklist
- I wrote one pure function that always returns the same result for the same input
- I completed: refactor one function so it accepts a dependency as an argument instead of hardcoding it
- I completed: split one small feature into a logic function and a UI wiring function, then explain which part became easier to test
- I tested the final result myself instead of guessing that it works
