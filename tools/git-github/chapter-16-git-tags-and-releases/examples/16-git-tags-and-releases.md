# Example — Creating and pushing a tag

```bash
git tag -a v1.0.0 -m "First stable release"
git push origin v1.0.0
```

A release note often answers three simple questions: what changed, what was fixed, and what people should know before updating.
