# Instructions

1. Set up a router at the app root.
2. Add three main routes and a fallback not-found route.
3. Create a shared navigation with `Link` components.
4. Add one dynamic route for a course details page.
5. Use `useNavigate` in one user action such as a "Go to profile" button.
