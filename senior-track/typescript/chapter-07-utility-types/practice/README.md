# Practice — Utility Types

## Exercise 1
Create a `User` type and then make a `UserSummary` type with `Pick` that keeps only `id` and `name`.

## Exercise 2
Create a `ProfileUpdate` type with `Partial` so you can update only some fields at a time.

## Exercise 3
Create a `Readonly` version of a settings object and confirm TypeScript warns you if you try to change it.

## Done Checklist
- I completed all three exercises myself
- I tested the result instead of guessing
- I can explain what each type is trying to model
- I know which part I need to practice again
