# Practice — Page Structure

## Exercise 1
Create a page with `header`, `main`, and `footer` sections.

## Exercise 2
Add two `section` elements inside `main`, each with its own heading.

## Exercise 3
Rebuild the page so someone can understand the structure just by reading the HTML tags.

## Done Checklist
- I created a page with `header`, `main`, and `footer` sections
- I added two `section` elements inside `main`, each with its own heading
- I completed: rebuild the page so someone can understand the structure just by reading the HTML tags
- I tested the final result myself instead of guessing that it works
