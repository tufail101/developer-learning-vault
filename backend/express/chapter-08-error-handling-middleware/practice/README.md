# Practice — Error Handling Middleware

## Exercise 1
Create one route that passes an error to `next()` instead of sending a normal response.

## Exercise 2
Add error middleware that catches the error and returns a JSON message.

## Exercise 3
Change the response status code from `500` to `400` for one custom validation error and compare the result.

## Done Checklist
- I completed all three exercises myself
- I tested the output after each change
- I can explain what this chapter does in plain language
- I know which part still feels unclear if I need to review it again
