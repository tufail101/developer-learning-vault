"""This example explains Python's role around browser-based work."""

roles = [
    "prepare data before it reaches the browser",
    "run a backend server",
    "automate repeated web tasks",
]

for role in roles:
    print(f"Python can: {role}")
