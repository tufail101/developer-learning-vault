# Practice — Transitions And Animations

## Exercise 1
Create a button that changes color smoothly on hover using `transition`.

## Exercise 2
Build a card that lifts slightly with `transform` when hovered.

## Exercise 3
Create a simple `@keyframes` animation that fades or slides one element into view.

## Done Checklist
- I created a button that changes color smoothly on hover using `transition`
- I built a card that lifts slightly with `transform` when hovered
- I created a simple `@keyframes` animation that fades or slides one element into view
- I tested the final result myself instead of guessing that it works
