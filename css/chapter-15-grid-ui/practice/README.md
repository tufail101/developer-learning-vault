# Practice — Grid UI

## Exercise 1
Create a simple two-column dashboard layout using CSS Grid.

## Exercise 2
Build a product or blog card grid with `repeat()` and `minmax()`.

## Exercise 3
Switch the grid to `auto-fit` or `auto-fill` and compare how the cards behave when the page width changes.

## Done Checklist
- I created a simple two-column dashboard layout using CSS Grid
- I built a product or blog card grid with `repeat()` and `minmax()`
- I completed: switch the grid to `auto-fit` or `auto-fill` and compare how the cards behave when the page width changes
- I tested the final result myself instead of guessing that it works
