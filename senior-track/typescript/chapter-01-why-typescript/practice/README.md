# Practice — Why TypeScript

## Exercise 1
Convert one small JavaScript function into TypeScript by adding parameter and return types.

## Exercise 2
Create a typed user object and log it from a `.ts` file.

## Exercise 3
Make one deliberate type mistake, read the error message, and then fix it so you can see what TypeScript catches for you.

## Done Checklist
- I completed: convert one small JavaScript function into TypeScript by adding parameter and return types
- I created a typed user object and log it from a `.ts` file
- I completed: make one deliberate type mistake, read the error message, and then fix it so you can see what TypeScript catches for you
- I tested the final result myself instead of guessing that it works
