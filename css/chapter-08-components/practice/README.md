# Practice — Components

## Exercise 1
Build a reusable button style class and apply it to two buttons.

## Exercise 2
Create a simple card component with a heading, paragraph, and button.

## Exercise 3
Make three cards on the page that share the same base styles but show different text content.

## Done Checklist
- I built a reusable button style class and apply it to two buttons
- I created a simple card component with a heading, paragraph, and button
- I completed: make three cards on the page that share the same base styles but show different text content
- I tested the final result myself instead of guessing that it works
