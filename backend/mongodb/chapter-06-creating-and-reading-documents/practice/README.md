# Practice — Creating And Reading Documents

## Exercise 1
Create one new document with `Model.create()` and log the returned result.

## Exercise 2
Use `Model.find()` to read every document in the collection.

## Exercise 3
Use `Model.findOne()` to read just one matching document.

## Done Checklist
- I completed all three exercises myself
- I tested the output after each change
- I can explain what this chapter does in plain language
- I know which part still feels unclear if I need to review it again
