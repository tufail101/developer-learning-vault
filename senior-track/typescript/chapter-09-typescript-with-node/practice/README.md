# Practice — TypeScript With Node

## Exercise 1
Create a typed config object with fields like `port`, `databaseUrl`, and `appName`.

## Exercise 2
Write a function that accepts a typed user object and returns a formatted log string.

## Exercise 3
Create a small array of typed records and write a function that finds one item by id.

## Done Checklist
- I completed all three exercises myself
- I tested the result instead of guessing
- I can explain what each type is trying to model
- I know which part I need to practice again
