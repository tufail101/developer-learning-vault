# Practice — Responsive Design

## Exercise 1
Create a two-column layout that looks good on a wide screen.

## Exercise 2
Add a media query so the layout becomes one column below a smaller width.

## Exercise 3
Test the page at multiple widths and write down the breakpoint where the layout changes.

## Done Checklist
- I created a two-column layout that looks good on a wide screen
- I added a media query so the layout becomes one column below a smaller width
- I completed: test the page at multiple widths and write down the breakpoint where the layout changes
- I tested the final result myself instead of guessing that it works
