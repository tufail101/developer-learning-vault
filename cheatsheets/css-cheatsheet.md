# CSS Cheatsheet

- selector + declaration block = CSS rule
- `color` changes text color
- `background` changes background
- `margin` adds outside space
- `padding` adds inside space
- `border` draws an edge
- `display: flex` enables flexible layout
- `display: grid` enables grid layout
- `@media` helps responsive design
- `transition` animates property changes
