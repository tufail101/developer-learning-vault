# Practice — Modern JavaScript

## Exercise 1
Use an arrow function to double every number in an array with `.map()`.

## Exercise 2
Use object destructuring to pull `name` and `price` from a product object and log both values.

## Exercise 3
Create one function that uses a template literal to return a readable summary sentence from an object.

## Done Checklist
- I used an arrow function to double every number in an array with `.map()`
- I used object destructuring to pull `name` and `price` from a product object and log both values
- I created one function that uses a template literal to return a readable summary sentence from an object
- I tested the final result myself instead of guessing that it works
