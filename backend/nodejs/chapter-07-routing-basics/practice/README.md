# Practice — Routing Basics

## Exercise 1
Build a server that handles `/`, `/about`, and `/contact` with three different responses.

## Exercise 2
Add a `404` response for any route you did not define and test it with a made-up URL.

## Exercise 3
Refactor the route checks so the logic reads clearly from top to bottom and each route is easy to spot.

## Done Checklist
- I built a server that handles `/`, `/about`, and `/contact` with three different responses
- I added a `404` response for any route you did not define and test it with a made-up URL
- I completed: refactor the route checks so the logic reads clearly from top to bottom and each route is easy to spot
- I tested the final result myself instead of guessing that it works
