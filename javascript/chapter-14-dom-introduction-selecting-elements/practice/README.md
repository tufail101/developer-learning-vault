# Practice — DOM Introduction And Selecting Elements

## Exercise 1
Create an HTML page with an `h1`, two paragraphs, and a button. Select the `h1` by ID and log it.

## Exercise 2
Use `querySelectorAll()` to select all paragraphs and log each paragraph text in a loop.

## Exercise 3
Select one child element, then log its `parentElement` and `nextElementSibling` so you can see DOM traversal in action.

## Done Checklist
- I created an HTML page with an `h1`, two paragraphs, and a button. Select the `h1` by ID and log it
- I used `querySelectorAll()` to select all paragraphs and log each paragraph text in a loop
- I completed: select one child element, then log its `parentElement` and `nextElementSibling` so you can see DOM traversal in action
- I tested the final result myself instead of guessing that it works
