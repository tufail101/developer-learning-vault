# Practice — Accessibility

## Exercise 1
Add meaningful `alt` text to two images on a practice page.

## Exercise 2
Build a form where every input has a visible label.

## Exercise 3
Create one icon-only button and give it an `aria-label` so someone using assistive technology can understand it.

## Done Checklist
- I added meaningful `alt` text to two images on a practice page
- I built a form where every input has a visible label
- I created one icon-only button and give it an `aria-label` so someone using assistive technology can understand it
- I tested the final result myself instead of guessing that it works
