# Expected Features — Headline Extractor

- The script runs with `python3 headline_extractor.py`
- It parses HTML with BeautifulSoup using `html.parser`
- It finds repeated headline elements with a selector
- It prints each headline clearly on its own line
- It skips empty items instead of printing messy blank output
