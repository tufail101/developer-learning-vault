# Practice — Functions And Generics

## Exercise 1
Write a typed function that formats a username into a greeting message.

## Exercise 2
Create a generic `getFirstItem<T>` helper for arrays.

## Exercise 3
Use the same generic helper with both a string array and a number array so you can see the type stay accurate in both cases.

## Done Checklist
- I wrote a typed function that formats a username into a greeting message
- I created a generic `getFirstItem<T>` helper for arrays
- I used the same generic helper with both a string array and a number array so you can see the type stay accurate in both cases
- I tested the final result myself instead of guessing that it works
