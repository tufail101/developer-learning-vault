# Practice — Type Narrowing And Guards

## Exercise 1
Write a function that accepts `string | number` and uses `typeof` to handle each case safely.

## Exercise 2
Create a union type for a successful API response and an error response, then narrow it with an `if` check.

## Exercise 3
Write a custom type guard for a small union type and reuse it in one function.

## Done Checklist
- I completed all three exercises myself
- I tested the result instead of guessing
- I can explain what each type is trying to model
- I know which part I need to practice again
