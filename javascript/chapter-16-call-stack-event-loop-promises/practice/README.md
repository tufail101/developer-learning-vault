# Practice — Call Stack, Event Loop, And Promises

## Exercise 1
Write a script that logs `A`, sets a timeout, and logs `B`. Predict the order before running it.

## Exercise 2
Create a resolved promise with `Promise.resolve()` and compare its log order with a `setTimeout` callback.

## Exercise 3
Build a small async function that waits for fake data with a promise and then updates a message on the page.

## Done Checklist
- I wrote a script that logs `A`, sets a timeout, and logs `B`. Predict the order before running it
- I created a resolved promise with `Promise.resolve()` and compare its log order with a `setTimeout` callback
- I built a small async function that waits for fake data with a promise and then updates a message on the page
- I tested the final result myself instead of guessing that it works
