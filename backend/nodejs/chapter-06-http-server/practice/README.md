# Practice — HTTP Server

## Exercise 1
Create a basic Node.js HTTP server that responds with `Server is running` in the browser.

## Exercise 2
Change the response so it sends a small HTML string with a heading and paragraph instead of plain text.

## Exercise 3
Add a second route check so `/about` returns a different message from `/`. Open both in the browser and confirm the outputs.

## Done Checklist
- I created a basic Node.js HTTP server that responds with `Server is running` in the browser
- I completed: change the response so it sends a small HTML string with a heading and paragraph instead of plain text
- I added a second route check so `/about` returns a different message from `/`. Open both in the browser and confirm the outputs
- I tested the final result myself instead of guessing that it works
