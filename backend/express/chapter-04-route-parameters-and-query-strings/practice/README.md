# Practice — Route Parameters And Query Strings

## Exercise 1
Create a route like `/users/:id` and return the captured ID in the response.

## Exercise 2
Add a query string such as `?role=admin` and return that value too.

## Exercise 3
Build one route that uses both a route parameter and a query string in the same response so you can compare the two.

## Done Checklist
- I completed all three exercises myself
- I tested the output after each change
- I can explain what this chapter does in plain language
- I know which part still feels unclear if I need to review it again
