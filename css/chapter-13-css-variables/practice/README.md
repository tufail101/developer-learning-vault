# Practice — CSS Variables

## Exercise 1
Define three CSS variables in `:root` for background, text, and accent colors.

## Exercise 2
Use those variables in a button and card component.

## Exercise 3
Create a second theme by changing the variable values and confirm the components update without changing the component rules themselves.

## Done Checklist
- I completed: define three CSS variables in `:root` for background, text, and accent colors
- I used those variables in a button and card component
- I created a second theme by changing the variable values and confirm the components update without changing the component rules themselves
- I tested the final result myself instead of guessing that it works
