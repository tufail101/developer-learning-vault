# Practice — Methods And Scope

## Exercise 1
Write a method called `doubleNumber` that returns a number times two.

## Exercise 2
Write a method called `introduce` that takes a name and city and prints a sentence.

## Exercise 3
Create a variable inside `main` and another inside a helper method, then notice they do not share scope automatically.

## Done Checklist
- I completed all three exercises myself
- I tested the result after each small change
- I can explain what this chapter is for in plain language
- I know which part to review if something still feels fuzzy
