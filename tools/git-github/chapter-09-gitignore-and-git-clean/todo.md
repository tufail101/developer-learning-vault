# Todo

- Add your most common local-only files and folders to a personal `.gitignore` checklist.
- Practice the difference between ignored files and already tracked files in a throwaway repo.
- Run `git clean -n` in a safe test folder until the command feels familiar before using it in real work.
